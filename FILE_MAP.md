# Kiwitown Electrical — File Map
_Generated by Claude Code, 2026-05-26_

## Root Config
| File | Description |
|------|-------------|
| `next.config.mjs` | Next.js + @ducanh2912/next-pwa config. Service worker in production, disabled in dev. |
| `tailwind.config.ts` | Tailwind config with custom tokens: bg (#0B0D12), brand (#00AEEF), surface, border, text-primary/secondary/muted. |
| `postcss.config.mjs` | PostCSS config for Tailwind. |
| `tsconfig.json` | TypeScript config. Path alias `@/` → project root. |
| `.env.local.example` | Template for required env vars — copy to .env.local and fill in Supabase credentials. |
| `HANDOFF.md` | Agent handoff notes — what was built, what's next. |
| `FILE_MAP.md` | This file — full project file inventory. |
| `CLAUDE.md` | Claude Code project instructions, stack rules, env var reference. |

## App (Next.js App Router)
| File | Description |
|------|-------------|
| `app/layout.tsx` | Root layout. DM Sans + Barlow Condensed + Satisfy from next/font/google. CSS variables: --font-dm-sans, --font-barlow, --font-satisfy. |
| `app/globals.css` | Global CSS: Tailwind base + custom scrollbar + foreman animation keyframes (checkPop, fadeUp, toastDrop, kePulse) + helper classes (row-btn, submit-btn, cat-header-btn, check-pop, fade-up, toast-anim, ring-pulse). |
| `app/page.tsx` | Root route (force-dynamic). Reads Supabase session → redirects to /login, /pending, or role dashboard. |
| `app/login/page.tsx` | Phone OTP login flow (client component). NZ number normalisation. |
| `app/pending/page.tsx` | Shown to authenticated users with no role assigned. |
| `app/foreman/page.tsx` | Server component. Fetches auth user + profile + existing weekly submission for current ISO week. Passes to ForemanDashboard. |
| `app/foreman/dashboard.tsx` | Client component — full foreman dashboard UI. Progress ring, 5 colour-coded category groups (Operations/Quality/Finance/Safety/People), 10 checklist items, collapsible reflection notes, mobile drawer nav, desktop nav, profile panel, toast, offline banner, localStorage persistence, submit → /api/weekly-submission. |
| `app/admin/page.tsx` | Admin dashboard (placeholder data — not yet wired to Supabase). |
| `app/sparky/page.tsx` | Sparky KPI scorecard (placeholder "--" values — not yet wired). |

## API Routes
| File | Description |
|------|-------------|
| `app/api/weekly-submission/route.ts` | POST: upserts foreman weekly checklist + notes to weekly_submissions table (idempotent per week). GET: returns submission history for the authenticated foreman. Role-gated to foreman. |

## Lib
| File | Description |
|------|-------------|
| `lib/supabase/client.ts` | Browser Supabase client (createBrowserClient). Used in client components. |
| `lib/supabase/server.ts` | Server Supabase client (createServerClient + cookie adapter). Used in server components, middleware, API routes. |
| `lib/types.ts` | Shared TypeScript types — Profile, WeeklySubmission, KpiEntry. |

## Middleware
| File | Description |
|------|-------------|
| `middleware.ts` | Runs on every non-static request. Validates Supabase session. Unauthenticated → /login. Role routing in app/page.tsx. |

## Public / PWA
| File | Description |
|------|-------------|
| `public/manifest.json` | PWA manifest — name, short_name, icons (192/512), theme_color (#00AEEF). |
| `public/icons/README.md` | Placeholder — icon-192x192.png + icon-512x512.png not yet added. |
| `public/sw.js` | Service worker generated by @ducanh2912/next-pwa. |
| `public/workbox-*.js` | Workbox runtime generated by @ducanh2912/next-pwa. |

## Supabase
| File | Description |
|------|-------------|
| `supabase/migrations/001_initial.sql` | Full schema: profiles, weekly_submissions, kpi_entries. RLS on all tables. Run in Supabase SQL Editor. |

## Planned API Routes (not yet built)
| Path | Purpose |
|------|---------|
| `app/api/kpi/route.ts` | Admin enters monthly KPI figures per sparky |
| `app/api/users/route.ts` | Admin lists users, assigns roles |
